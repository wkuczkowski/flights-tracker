# Agent owns interpretation of travel intent

The Agent interprets subjective travel intent such as “somewhere warm” or “a good deal”, while the CLI remains a stateless provider of objective exploration and live-search facts. This keeps contextual judgement close to the conversation and avoids embedding opaque, hard-to-evolve scoring rules or user preferences in the CLI.
